import SignIn from "./signIn";

/**
 * Render the authentication home UI.
 *
 * @returns A JSX element that renders the `SignIn` component.
 */
export default function AuthHome() {
  return <SignIn />;
}
